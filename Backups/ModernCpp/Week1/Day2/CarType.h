#ifndef CARTYPE_H
#define CARTYPE_H

/*
    Data: real-value(numbers which can have minimun maximum values)
          categorical(months, days, value can be one value out of n number of options)
*/

enum class CarType{
    SEDAN,
    SUV,
    HATCHBACK
};

#endif // CARTYPE_H
