/*
    1) It enhances the language with a brank new fetaure (lambdas)
    2)
        a) It improves an existing freature (pointer --> smart pointer)
        b) It improves an existing feature
        inspired from C language with had a drwaback (enum calss)
*/

#include <iostream>
int main(){
    int *ptr = NULL;
    int n1 = NULL; //this is also valid
    // so the defference between the NULL and nullptr is that
    //nullptr is dedicated to pointer and NULL is just 0X000H any address which can come
    //in any type of the data
    int *ptr2 = nullptr;
    // int n2 = nullptr //This is not valid;
}